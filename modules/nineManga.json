{
  "module_infos": {
    "module_name": "NineManga",
    "img": "https://www.ninemanga.com/files/img/ninemanga.png",
    "initials": "NM",
    "language": "FR",
    "developer": "Frizrat",
    "desc": "Module for NineManga"
  },
  "main": {
    "request": {
      "url": "https://fr.ninemanga.com/",
      "headers": {},
      "proxies": false
    },
    "javascript_config": {
      "remove": true,
      "javascript": "function main(document) { let sections = []; [['.pop_update', 'font'], ['.rightbox', 'span']].forEach(([s1, s2]) => { let elements = []; document.querySelectorAll(s1+' > ul').forEach((section, i) => { section.querySelectorAll('li').forEach(element => { let a = element.querySelector('a'); let img = a.querySelector('img'); elements.push(new Element( new Link(a.href+'?waring=1'), img.alt, element.querySelector('img').src, '', element.querySelector(s2).textContent.replace(img.alt, '').trim() )); }); sections.push(new Section(document.querySelectorAll(s1+' h1')[i].textContent, elements)); }); }); return sections;}"
    }
  },
  "search": {
    "request": {
      "url": "https://fr.ninemanga.com/search/?wd=${search.s}&page=${search.page}.html",
      "first_page": 1,
      "page_spacing": 1,
      "headers": {},
      "proxies": false
    },
    "javascript_config": {
      "remove": true,
      "javascript": "function search(document) { let elements = Array.from(document.querySelectorAll('.bookinfo')).map(element => { let a = element.querySelector('a.bookname'); return new Element(new Link(a.href+'?waring=1'), a.textContent, element.querySelector('img').src, element.querySelector('span').textContent, element.querySelector('.chaptername').textContent.replace(a.textContent, '').trim(), '', [new Genre_Player(element.querySelector('p').textContent)]) }); return [new Section('', elements, 'portrait', 'vertical')];}"
    }
  },
  "info": {
    "request": {
      "headers": {},
      "proxies": false
    },
    "javascript_config": {
      "remove": true,
      "javascript": "/* Complete and remove commentary */\n/* Don't use '\"' or put a '\\' before */\n/* Never forget ';' */\n/* Go on https://beautifier.io/ to format your code */\n/* Go on https://www.textfixer.com/html/compress-html-compression.php to compress your code */\nfunction info(document) { let infos = new Infos; infos.title = '';/* Title */ infos.img = '';/* Image */ infos.type = '';/* Type next to Flouflix icon */ infos.details = [];/* Details under the title */ infos.desc = '';/* Desc */ infos.genres = [new Genre_Player('')]; /* Genres under the desc */ infos.sections.push(new Section(/* section name */, /* elements */, /* element type */, /* scrolling sens */)) return infos}"
    }
  },
  "video": {
    "request": {
      "headers": {},
      "proxies": false
    },
    "javascript_config": {
      "remove": true,
      "javascript": "/* Complete and remove commentary */\n/* Don't use '\"' or put a '\\' before */\n/* Never forget ';' */\n/* Go on https://beautifier.io/ to format your code */\n/* Go on https://www.textfixer.com/html/compress-html-compression.php to compress your code */\nfunction video(document) { let players = [new Genre_Player('', '')]; /* Player name (not necessary) */, /* link */ return players;}"
    }
  },
  "genre": {
    "request": {
      "headers": {},
      "proxies": false
    },
    "javascript_config": {
      "remove": true,
      "javascript": "/* Complete and remove commentary */\n/* Don't use '\"' or put a '\\' before */\n/* Never forget ';' */\n/* Go on https://beautifier.io/ to format your code */\n/* Go on https://www.textfixer.com/html/compress-html-compression.php to compress your code */\nfunction genre(document) { let sections = []; for (let section of /* sections */) { let elements = []; for (let film of /* elements */) { elements.push(new Element( new Link(''), /* Link */, /* Headers */, /* Proxies */ '', /* Title */ '',/* Image */, /* portrait | landscape | square */ '',/* Detail top green */ '',/* Detail top surrounded */ '',/* Detail top right */ [new Genre_Player()]/* Deatil bottom */ } sections.push(new Section(/* section name */, elements, /* element type */, /* scrolling sens */)) } return sections;}"
    }
  }
}
